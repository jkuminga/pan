<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>날짜별 전적</title>
    <style>
        :root {
            --bg: #0f172a;
            --panel: #111827;
            --muted: #94a3b8;
            --text: #e2e8f0;
            --accent: #38bdf8;
            --border: #1f2937;
        }
        * { box-sizing: border-box; }
        body {
            margin: 0;
            padding: 0;
            background: radial-gradient(120% 120% at 20% 20%, #0b1224, #0f172a 60%, #0b1020);
            color: var(--text);
            font-family: "Pretendard", "Segoe UI", system-ui, -apple-system, sans-serif;
        }
        .page {
            max-width: 1100px;
            margin: 0 auto;
            padding: 24px;
        }
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 16px;
        }
        h1 { margin: 0; }
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 14px;
        }
        .card {
            background: rgba(17, 24, 39, 0.9);
            border: 1px solid var(--border);
            border-radius: 14px;
            padding: 14px;
            box-shadow: 0 10px 28px rgba(0,0,0,0.3);
            display: grid;
            gap: 8px;
        }
        .day { font-size: 16px; font-weight: 700; }
        .count { color: var(--muted); font-size: 14px; }
        .view-btn {
            justify-self: start;
            padding: 8px 12px;
            border-radius: 10px;
            background: linear-gradient(135deg, #38bdf8, #22d3ee);
            color: #0b1220;
            text-decoration: none;
            font-weight: 700;
            border: none;
        }
        .empty {
            border: 1px dashed var(--border);
            border-radius: 12px;
            padding: 18px;
            text-align: center;
            color: var(--muted);
            background: rgba(17, 24, 39, 0.6);
        }
    </style>
</head>
<body>
    <div class="page">
        <header>
            <h1>날짜별 전적</h1>
            <a href="/history" style="color: var(--muted); text-decoration:none;">전체 전적</a>
        </header>

        <% const datesSafe = dates || []; %>
        <% if (datesSafe.length === 0) { %>
            <div class="empty">데이터가 없습니다.</div>
        <% } else { %>
            <div class="grid">
                <% datesSafe.forEach((d) => { %>
                    <div class="card">
                        <div class="day"><%= d.day %></div>
                        <div class="count">플레이된 게임: <%= d.count %>회</div>
                        <a class="view-btn" href="/history/search?date=<%= encodeURIComponent(d.day) %>">보기</a>
                    </div>
                <% }) %>
            </div>
        <% } %>
    </div>
</body>
</html>
